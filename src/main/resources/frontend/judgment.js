import $ from 'jquery';
//import Vue from 'vue';
//import Counter from './components/Counter';

console.log('This is only reached if the Vue import is commented out.');
const el = $('#counter');
console.log(el);

/*
new Vue({
	el: '#counter',
	  
	render(createElement) {
		return createElement(Counter);
	}
});
*/
